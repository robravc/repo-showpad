export class Effect {
    effect: string
    shortEffect: string
    
    constructor(effect: string, shortEffect: string) {
        this.effect = effect
        this.shortEffect = shortEffect
    }
}